import MapPageConnected from './index';

describe('Map page component', () => {
  test('#it should exist Map page component', () => { //eslint-disable-line
    expect(MapPageConnected).toBeTruthy();
  });
});
